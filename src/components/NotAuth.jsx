import React from "react";
import { Box } from "@mui/system";
import { Typography } from "@mui/material";

const NotAuth = () => {
  return (
    <Box>
      <Typography variant="h4">You are not authenticated</Typography>
    </Box>
  );
};

export default NotAuth;
